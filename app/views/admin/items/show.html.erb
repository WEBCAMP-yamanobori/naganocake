<strong>商品詳細</strong>

<div>
  <%= attachment_image_tag @item, :image, :fill, 250, 250, fallback: "22047646_s.jpg", size: "250x250" %>
</div>

<div>
  <p>商品名 <%= @item.name %></p>
  <p>商品説明 <%= @item.introduction %></p>
  <p>ジャンル</p>
  <p>税込価格（税抜価格）</p>
  <p>販売ステータス
    <% if @item.is_active == true %>
    販売中
    <% else %>
    販売停止中
    <% end %>
  </p>
</div>

<div>
  <%= link_to edit_admin_item_path do %>
    編集
  <% end %>
</div>