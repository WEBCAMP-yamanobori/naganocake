<div class="container">
  <div class="row text-right my-5">
    <div class="col-sm-2">
      <strong>商品詳細</strong>
    </div>
  </div>
    
  <div class="row my-3">
    <div class="col-sm-4">
      <div>
        <%= attachment_image_tag @item, :image, :fill, 350, 250, fallback: "22047646_s.jpg", size: "350x250" %>
      </div>
    </div>
    <div class="col-sm- offset-1">
      <div>
        <p>商品名 <%= @item.name %></p>
        <p class="my-4">商品説明 <%= @item.introduction %></p>
        <p class="mb-4">ジャンル <%= @item.genre.name %></p>
        <p class="price", style="margin:0">税込価格<%= @item.add_tax_non_taxed_price.to_s(:delimited) %>(<%= @item.non_taxed_price.to_s(:delimited) %>)円</p>
        <p class="mb-4">（税抜価格）</p>
        <p>販売ステータス
          <% if @item.is_active == true %>
          <p class="color text-success", style="margin:0">販売中</div>
          <% else %>
          <p class="color text-secondary", style="margin:0">販売停止中</div>
          <% end %>
        </p>
      </div>
    </div>
    
    <div class="row my-3">
      <div class="col-sm-12">
        <div class="text-center">
          <%= link_to "編集", edit_admin_item_path, class:"btn btn-success btn-lg col-2" %>
        </div>
      </div>
    </div>
  </div>
</div>